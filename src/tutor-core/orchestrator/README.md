# Tutor orchestrator

Pure, domain-neutral next-action policy. It consumes an authority decision, immutable graph/projection versions, ranked ready candidates, learner controls, and structured recent evidence. It returns an inspectable current-node assistant slice, one-variable adaptation, bounded callback/fallback, conservative mastery update, and delayed-check schedule.
