# Activity validation

Pre-exposure pedagogical validation for generated activity configuration and small application code. Deterministic gates always run first; optional bounded judgments can add evidence but cannot override a deterministic failure. Every result is a provenance-bound `tutor.activity-validation/v1` report and resolves to publish, reviewed fallback, human review, or clean stop.
